#include <iostream>
const int ArrSize=10;
int main()
{
	using namespace std;
	char fname[ArrSize];
	char lname[ArrSize];
	cout<<"Enter your first name:";
	cin.getline(fname,ArrSize);
	cout<<endl<<"Enter your last name:";
	cin.get(lname,ArrSize);
	cout<<endl<<"Here's the information in a single string "
		<<lname<<", "<<fname;
	cin.get();
	return 0;
}
